# Advanced FPM configuration for microservices
# Use with Nginx as reverse proxy for optimal performance

# Multiple database drivers
# Caching layers
# API and networking essentials

PHP_EXTENSIONS=pdo_mysql,pdo_pgsql,redis,memcached,opcache,zip,intl,bcmath,soap,sockets
